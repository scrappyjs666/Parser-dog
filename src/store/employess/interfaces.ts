export interface IUser {
  name: string;
  LastName: string;
}

export interface IPerson {
  FirstName: string;
  LastName: string;
  Birthday: string;
  Gender: string;
  Position: string;
  Zeitplan: string;
  id?: string;
}
